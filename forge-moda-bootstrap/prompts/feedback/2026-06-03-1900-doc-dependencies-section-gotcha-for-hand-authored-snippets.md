---
timestamp: 2026-06-03T20:00:00Z
session_id: eager-nightingale-626224
prompt_modified: 2026-06-03T19:00:00Z
status: success
---

# Doc-only — `# Dependencies` gotcha for hand-authored snippets — feedback

## §0 Commit pointers

- **Doc home chosen:** `~/projects/forge-client-obsidian/INSTALL.md`
- **Commit:** `bf157e4` on `main`
- **Push:** `origin/main` (15fa86e..bf157e4)
- **Section added:** new `## Authoring notes — hand-authored snippets` between `## Network requirements` and `## Troubleshooting` (INSTALL.md lines 102–129)
- No release cut; no version bump; no code change. `npm test` stays at 215/215.

### Auto-verified grep gates (per prompt §Tests)

```
'Dependencies' count: 0 → 5
'v0.2.41'      count: 0 → 1
'wikilink'     count: 0 → 3
```

All three gates clear (`≥ 1`).

## §1 Full quoted text of the new note as it landed

```markdown
## Authoring notes — hand-authored snippets

**`# Dependencies` is engine-emitted, not author-emitted.** When you
write a snippet by hand (creating `.md` files in your vault without
going through `/generate`), the snippet won't have a `# Dependencies`
block at the bottom of its body. That's expected — per constitution
B7, the block is generated by static analysis on the Python facet at
`/generate` time, not authored manually.

**What you lose without it.** The wikilink right-click freeze
affordance (v0.2.41+) needs wikilinks in the snippet body to operate
on. If your hand-authored snippet's body has no `[[snippet_name]]`
references — only backticked code like
`context.compute('snippet_name', ...)` — the right-click menu has
nothing to target, so the freeze / unfreeze items won't appear.
**Workaround**: append a `# Dependencies` block manually with the
wikilinks your Python calls. For example:

​```
# Dependencies

[[callee_one]] [[callee_two]]
​```

The block doesn't change snippet behavior; it only enables the
right-click affordance. The `Cmd+P` → "Forge: Freeze edge"
command-palette path works regardless of whether `# Dependencies` is
present.
```

(Zero-width spaces inserted around the inner fence above so this
markdown renders cleanly inside a fenced-code block.)

## §2 Judgment call on doc home

The prompt's preference order was:

1. `forge/docs/authoring.md` or `forge-client-obsidian/docs/authoring.md` — **doesn't exist** in either repo.
2. `INSTALL.md` in `forge-client-obsidian` — **exists**, chosen.
3. `forge/docs/specs/constitution.md` — the prompt flagged it as probably wrong (invariants vs tips), and the constitution is read by maintainers not students.
4. README — last resort.

**Chose preference 2.** Justification:
- Closed-beta students read `INSTALL.md`; the note is operational ("if you hit this, here's the workaround"), not architectural.
- `INSTALL.md` already has `## Network requirements` (a similar concrete-state explanation), so the new `## Authoring notes` section fits the same shape and adjacency.
- Placed between `## Network requirements` and `## Troubleshooting` rather than inside `## Troubleshooting` because this is plugin-state knowledge ("here's how to author for the wikilink affordance"), not a troubleshooting symptom ("the right-click menu doesn't work").

No "obviously-right home didn't exist" flag — preference 2 worked cleanly.

## §3 User-side smoke checklist

> Doc-only drain. The smoke is short: visually confirm the new section reads cleanly, and confirm the workaround actually unblocks the affordance.

**Pre-conditions:**

- Access to `https://github.com/frmoded/forge-client-obsidian/blob/main/INSTALL.md` (browser).
- Optionally: `~/forge-vaults/smoke-v0.2.13/` open in Obsidian with the v0.2.44 plugin install (carried over from the eager-nightingale drain triple).

### Steps

1. **Read the new section on GitHub.** Browser → https://github.com/frmoded/forge-client-obsidian/blob/main/INSTALL.md → scroll to **Authoring notes — hand-authored snippets**.
   Expected: two paragraphs render in order, with the fenced code-block in the middle showing the `# Dependencies` example as proper markdown (block heading + two wikilinks on a line below).
   Interpretation: text wraps oddly, code block doesn't render → markdown escape issue; flag and we'll fix in a follow-up.

2. **(Optional, but the load-bearing user-facing claim.)** If you have a hand-authored snippet WITHOUT a `# Dependencies` block — e.g., a fresh `.md` file you create at the vault root with `type: action`, an English facet, and a Python facet that calls `context.compute('something', ...)` but has no `[[something]]` wikilink in the body — right-click anywhere in the snippet body in live preview.
   Expected: no `Forge: Freeze edge` / `Forge: Unfreeze edge` items appear (the v0.2.41+ helper correctly suppresses the menu when the cursor isn't inside a wikilink).
   Interpretation: items appear → the helper is matching something unexpected; flag.

3. **(Optional, completes the loop.)** Append the workaround block to the snippet from step 2:
   ```
   
   # Dependencies
   
   [[something]]
   ```
   Save (`Cmd+S`). Right-click on the new `[[something]]` wikilink in live preview.
   Expected: the `Forge: Freeze edge ... → something` and `Forge: Unfreeze edge ...` items now appear (subject to `something` being a known snippet — same gating as the v0.2.41 affordance).
   Interpretation: items appear → workaround works as documented. Items still absent → either `something` isn't a known snippet (check vault file tree), or the cursor isn't inside the wikilink span (right-click directly on the link text).

### Failure modes to watch for

- **Step 1 — section heading appears but the inner code-block renders as inline code or shows the zero-width spaces** → the markdown escape didn't survive. Fix: replace the zero-width-space delimiters in §1 with a different escape pattern (e.g., use four-space indented code instead of fenced code).
- **Step 2 — freeze items DO appear in a snippet body with no wikilinks** → the v0.2.41 helper's wikilink detection has a false positive. Capture the right-click position + the snippet body and flag.
- **Step 3 — items still absent after adding `# Dependencies`** → either the appended block's wikilink target isn't a known snippet (lookup via `app.metadataCache.getFirstLinkpathDest('something', ...)` in DevTools to verify) or the cursor wasn't inside the bracket span.

### End-state cleanup

If you created a throwaway hand-authored snippet for steps 2–3, delete it (or leave it; it's harmless).

## §2 followup — applying the smoke-automation feedback I just shipped

This drain was a clean test of the protocol amendments I proposed in `forge-core/feedback-cc-smoke-automation-default.md` earlier this session. The split:

**Auto-verified by CC:**
- Located doc-home candidates (preferences 1, 2, 3 surveyed in one Bash run).
- Confirmed `forge/docs/authoring.md` and `forge-client-obsidian/docs/authoring.md` don't exist.
- Confirmed INSTALL.md exists; surveyed its section structure.
- Baselined grep counts (`Dependencies: 0`, `v0.2.41: 0`, `wikilink: 0`).
- Wrote the new section via Edit.
- Re-grepped to verify gates cleared (`5`, `1`, `3`).
- Ran `npm test` to confirm suite is unchanged at 215/215.
- Committed + pushed.

**Deferred to user (user-side smoke):**
- Visual confirmation that the section renders correctly on GitHub.
- (Optional) Verify the workaround actually unblocks the affordance on a real hand-authored snippet — that's the load-bearing claim of the new doc.

Three numbered user-side steps total, all genuinely visual / live-Obsidian. Amendment B (typical 3–8 step bound) honored.
